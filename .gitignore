# generated delivery bundles
AP_Cyber*.zip
AP_CSP*.zip

# placeholder stand-in for missing image asset
pipeline/phishing-login-page.png

# Judge0 API key config — never commit
pipeline/csp/judge0.config.json
